package gochecksum

func Validate(mod string, tag string) error {

	// Here we need to replicate:
	// source: github.com/open-policy-agent/opa
	// tag: v0.62.1
	// curl https://sum.golang.org/lookup/${source}:${tag}
	return nil
}
